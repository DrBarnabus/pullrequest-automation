import { debug, info, warning, error, getInput, setFailed } from '@actions/core';

export {
    debug as logDebug,
    info as logInfo,
    warning as logWarning,
    error as logError,
    getInput,
    setFailed
};
